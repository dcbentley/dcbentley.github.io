// Lets make a neat function
function darkness() { // Naming of function
    let element = document.body // Declaring variable and scope 
    element.classList.toggle("lights"); // Toggle class .lights in CSS
    console.log(document.body); // Console log check
}